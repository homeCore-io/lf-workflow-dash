page_title: homeCore Builds

repos:
  # ── Core ────────────────────────────────────────────────────────────────
  - repo: homeCore
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  # ── Plugins ─────────────────────────────────────────────────────────────
  - repo: hc-hue
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-yolink
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-lutron
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-sonos
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-wled
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-isy
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-zwave
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-caseta
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-thermostat
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  - repo: hc-ecowitt
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  # ── Clients ─────────────────────────────────────────────────────────────
  - repo: hc-tui
    owner: homeCore-io
    live-build: ci.yml
    other_workflows:
      - release.yml

  # Repos without CI/Release workflows yet (omitted until wired):
  #   hc-plugin-sdk-rs, hc-captest, hc-scripts (reusable workflows only),
  #   hc-web-leptos, hc-mcp, plus the inactive client variants.
